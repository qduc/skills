Feature request:

"My account’s weekly credit meter never appears in the terminal. This provider
does not include that number in normal inference replies, but its own command
line client can show it. Add a small status-bar meter that shows the percentage
used and, when available, the period reset date. It must not invent a zero when
no reading is available."

Find the relevant integration points and implement it. Add tests covering the
feature.
