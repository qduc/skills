Bug report from a user:

"Streaming replies come back fine, but the status bar never shows token
counts, the context gauge, or cost — for any provider that speaks the
chat-completions wire format. The model already emits a usage block at the
end of a stream, but my session never records it, so the UI has nothing to
render."

Find the cause and fix it. Add tests covering the fix.
